spring:
  application:
    name: user-service
  datasource:
    url: ${DB_URL:jdbc:postgresql://ep-gentle-meadow-ap4xj11l-pooler.c-7.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require}
    username: ${DB_USERNAME:neondb_owner}
    password: ${DB_PASSWORD:npg_cdaFb6Lg7BUp}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
server:
  port: 8002
security:
  jwt:
    secret: ${JWT_SECRET:a dashboard, debugger, or inspector used to decode}